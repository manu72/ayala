{
  "name": "ayala",
  "private": true,
  "version": "0.4.4",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:proxy": "npm --prefix proxy run dev",
    "deploy:proxy": "npm --prefix proxy run deploy",
    "build": "tsc && vite build",
    "verify:dist": "node scripts/check-dist-leaks.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:proxy": "cd proxy && npm test",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@types/easystarjs": "^0.1.29",
    "easystarjs": "^0.4.4",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "fake-indexeddb": "^6.2.5",
    "pngjs": "^7.0.0",
    "typescript": "^6.0.2",
    "vite": "^8.0.8",
    "vitest": "^3.2.4"
  }
}
